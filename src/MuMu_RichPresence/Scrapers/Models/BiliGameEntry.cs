namespace Dawn.MuMu.RichPresence.Scrapers.Models;

// There are a lot more properties, but we only need the ones listed below
public record BiliGameEntry(int GameBaseId,  string GameName, string? Icon);
